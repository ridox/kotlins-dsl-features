package ua.kotlin.dsl.java.not.have.demo1

val numbers: MutableList<Int> = mutableListOf(1, 2, 3)
numbers.add(4)
//val immutableList = listOf(1, 2, 3)
//immutableList.add(4)

//val readOnlyView: List<Int> = numbers
//readOnlyView.add(5)

/*for (i in 1..20) {
    if (i in 10..15) {
        println(i)
    }
}*/

/*val persons = mapOf(
    1 to Person("Person1", 31),
    2 to Person("Person2", 32),
    3 to Person("Person3", 33)
)

for ((id, person) in persons) {
    println("$id -> Name: ${person.name}, Age: ${person.age}")
}*/
